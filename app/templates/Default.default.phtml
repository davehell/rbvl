<div class="row">
  <div class="col-md-6">
    <div class="panel-box">
      <div class="row">
        <div class="col-md-12">
          {if isset($user)}
          <p>
            <a class="btn btn-default pull-right" href="{link Stranky:edit, 1}" title="Upravit text úvodní stránky"><span class="glyphicon glyphicon-pencil"></span> Upravit text</a>
          </p>
          {/if}
          {!$text|texy|vlna}
        </div>
      </div>
    </div> <!-- .panel-box -->
    <div class="row">
      <div class="col-md-6">
        <div class="panel-box">
          <div class="titles">
            <h4>Výsledky aktuálního kola</h4>
          </div>
          <ul>
            <li>Liga A - základní část<br><a href="#">Hrací den č. 5</a> - 15.2.2015</li>
            <li>Liga B - finále<br><a href="#">Hrací den č. 5</a> - 15.2.2015</li>
          </ul>
        </div>
      </div>
      <div class="col-md-6">
        <div class="panel-box">
          <div class="titles">
            <h4>Rozlosování příštího kola</h4>
          </div>
          <ul>
            <li>Liga A - základní část<br><a href="#">Hrací den č. 5</a> - 15.2.2015</li>
            <li>Liga B - finále<br><a href="#">Hrací den č. 5</a> - 15.2.2015</li>
          </ul>
        </div>
      </div>
    </div> <!-- .row -->
  </div> <!-- .col-md-6 -->
  <div class="col-md-3">
    <div class="panel-box">
      <div class="titles">
        <h4>Liga A - tabulka</h4>
      </div>
      {foreach $tabulkyA as $row}
        {if $skupinaA != $row->skupina_popis}
          {if $skupinaA !=  ""}
            </tbody></table>
          {/if}
          <h5 class="text-center">{$row->skupina_popis}</h5>
          {assign $poradi 1}
          <table class="table table-hover table-condensed">
          <tbody>
        {/if}
        <tr><td>{$poradi}.</td><td>{$row->druzstvo_nazev}</td><td>{$row->body}</td></tr>
        {assign $skupinaA $row->skupina_popis}
        <?php $poradi++?>
        {if $iterator->isLast()}</tbody></table>{/if}
      {/foreach}
    </div>
  </div> <!-- .col-md-3 -->
  <div class="col-md-3">
    <div class="panel-box">
      <div class="titles">
        <h4>Liga B - tabulka</h4>
      </div>
      {foreach $tabulkyB as $row}
        {if $skupinaB != $row->skupina_popis}
          {if $skupinaB !=  ""}
            </tbody></table>
          {/if}
          <h5 class="text-center">{$row->skupina_popis}</h5>
          {assign $poradi 1}
          <table class="table table-hover table-condensed">
          <tbody>
        {/if}
        <tr><td>{$poradi}.</td><td>{$row->druzstvo_nazev}</td><td>{$row->body}</td></tr>
        {assign $skupinaB $row->skupina_popis}
        <?php $poradi++?>
        {if $iterator->isLast()}</tbody></table>{/if}
      {/foreach}
    </div>
  </div> <!-- .col-md-3 -->
</div> <!-- .row -->

<!--
<p class="alert alert-warning">
  už nyní se můžete přihlašovat do nového ročníku „RB“VL. <strong>Uzávěrka přihlášek je 15.9.2014!</strong>
</p>

<p>
 Veškeré informace získáte na těchto webových stránkách v&nbsp;sekcích <a href="{plink Pravidla:}">Pravidla</a>
 a&nbsp;<a href="{plink Propozice:}">Propozice</a>. Přihlášku a&nbsp;soupisku najdete v&nbsp;sekci <a href="{plink Download:}">Ke stažení</a>.
</p>
-->
